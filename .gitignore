__pycache__/
*.py[cod]
.DS_Store

# Credentials never enter version control. The token lives in
# ~/.config/canvas-mcp/config.json
config.json
*.token
.env

# Local-only message catalogues. English is the shipped default; translations
# stay on the machine that uses them. A missing catalogue falls back to English,
# so nothing here is required to run the server.
canvas_mcp/i18n/zh.py
